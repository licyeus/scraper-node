import './scraper-test'
import './helpers-test'

// each item in array is descriptor of operation and yields a selection
// has, map, reduce, until, after, find, filter, reject, contains, attr, join, tap, split, inspect, text, children, is
